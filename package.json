{
  "name": "cna-summer-project",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd CNA_APP && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../CNA_APP && npm install",
    "build:frontend": "cd CNA_APP && npm run build",
    "deploy:prod": "git checkout main && git merge development && git push origin main"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
